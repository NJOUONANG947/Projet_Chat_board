# ============================================
# CONFIGURATION EMAIL - RESEND (OBLIGATOIRE)
# ============================================
# Pour que les candidats reçoivent RÉELLEMENT les emails, vous DEVEZ configurer Resend
# 
# 1. Créez un compte sur https://resend.com
# 2. Obtenez votre clé API dans le dashboard Resend
# 3. Remplacez les valeurs ci-dessous

RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# EMAIL_FROM : Adresse expéditrice des emails
# Option 1 (RECOMMANDÉ pour commencer) : Utiliser l'adresse de test Resend
# Cette adresse est fournie automatiquement - AUCUNE configuration nécessaire !
# En mode test, Resend n'envoie qu'à VOTRE adresse (celle du compte Resend).
EMAIL_FROM=onboarding@resend.dev

# RESEND_FROM_EMAIL : même rôle qu'EMAIL_FROM (prioritaire si les deux sont définis).
# Pour envoyer aux recruteurs et aux candidats, vous DEVEZ vérifier un domaine sur Resend
# puis utiliser une adresse de ce domaine ici (ex: noreply@votredomaine.com).
# RESEND_FROM_EMAIL=CareerAI <noreply@votredomaine.com>

# Adresse email qui reçoit les avis/feedback des utilisateurs (section Paramètres > Avis).
# Si non défini, les avis sont envoyés à l'adresse par défaut du projet.
# Pour que l'envoi d'avis fonctionne : définissez au minimum RESEND_API_KEY (et optionnellement EMAIL_FROM=onboarding@resend.dev).
# FEEDBACK_EMAIL=arhurnjouonang5@gmail.com

# Option 2 (Pour la production) : Utiliser votre propre domaine
# 1. Ajoutez votre domaine dans Resend Dashboard > Domains
# 2. Configurez les enregistrements DNS fournis par Resend
# 3. Attendez la vérification du domaine
# 4. Utilisez ensuite votre adresse personnalisée :
# EMAIL_FROM=noreply@votredomaine.com

# URL de base de votre application
NEXT_PUBLIC_BASE_URL=http://localhost:3000
# En production, remplacez par votre URL réelle :
# NEXT_PUBLIC_BASE_URL=https://votredomaine.com

# ============================================
# SUPABASE CONFIGURATION
# ============================================
NEXT_PUBLIC_SUPABASE_URL=votre_url_supabase
NEXT_PUBLIC_SUPABASE_ANON_KEY=votre_cle_anon_supabase
SUPABASE_SERVICE_ROLE_KEY=votre_cle_service_role_supabase

# ============================================
# GROQ AI CONFIGURATION
# ============================================
GROQ_API_KEY=votre_cle_groq

# ============================================
# GÉNÉRATION D'IMAGES (optionnel)
# ============================================
# Option 1 - GRATUIT : Hugging Face (Stable Diffusion)
# 1. Créez un compte sur https://huggingface.co
# 2. Settings → Access Tokens → New token (type "Read")
# 3. Collez le token ci-dessous
HUGGINGFACE_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Option 2 - PAYANT : OpenAI DALL·E 3 (utilisé seulement si HUGGINGFACE_TOKEN n'est pas défini)
# OPENAI_API_KEY=sk-votre_cle_openai

# CANDIDATURES AUTO - Adzuna France (optionnel) : https://developer.adzuna.com/signup
# ADZUNA_APP_ID=
# ADZUNA_APP_KEY=
